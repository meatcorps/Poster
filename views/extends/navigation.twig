    <!-- Fixed navbar -->
    <nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="{{path("index.home")}}">Poster!</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li {% if active_page == "home" %}class="active"{% endif %}><a href="{{path("index.home")}}">Posts</a></li>
                {% if is_granted('ROLE_ADMIN') %}
                <li {% if active_page == "admin.posts" %}class="active"{% endif %}><a href="{{path("admin.post.overview")}}">Manage posts</a></li>
                <li {% if active_page == "admin.users" %}class="active"{% endif %}><a href="{{path("admin.user.overview")}}">Manage admins</a></li>              
                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                {% if is_granted('ROLE_ADMIN') %}
                    <li><a href="{{path("admin.logout")}}">Logout</a></li>
                {% else %}
                    <li {% if active_page == "login" %}class="active"{% endif %}><a href="{{path("admin.login")}}">Login</a></li>
                {% endif %}   
            </ul>
        </div><!--/.nav-collapse -->
      </div>
    </nav>
    <p>&nbsp;</p>
    <p>&nbsp;</p>